# Para Cloudflare Pages usa: npm run deploy:cloudflare
# (wrangler pages deploy deploy --project-name=pupas)
# Este archivo se usa si despliegas como Worker con assets.
name = "pupas"
compatibility_date = "2026-02-01"
pages_build_output_dir = "./deploy"
